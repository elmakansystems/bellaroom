<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/10.15.7/sweetalert2.min.css" />
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/invoices.css">
    <link rel="stylesheet" href="/styles/responsive.css">
    <title>بيلا رووم |
        <%= title %>
    </title>
</head>


<body onload="resets()">
    <header id="main-nav-header">
        <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-e" dir="rtl">
            <div class="container-lg">
                <a class="navbar-brand" href="/">بيلا روم</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
                <div dir="text-right" class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">

                    <% if (user) { %>
                        <% if (user.isAdmin || user.isEditor) { %>
                           
                            <ul class="navbar-nav m-auto mb-2 mb-lg-0">
                                <li class="nav-item btn btn-outline-warning mx-1" style="border-color: transparent;">
                                    <a class="nav-link active" style="padding: 5px 20px;" aria-current="page" href="/">الرئيسية</a>
                                </li>
                                <% if (user.isAdmin) { %>
                                    <li class="nav-item btn btn-outline-warning mx-1" style="border-color: transparent;">
                                        <a class="nav-link active" style="padding: 5px 20px;" aria-current="page" href="/safe">الخزنة</a>
                                    </li>
                                    <% } %>
                            </ul>
                            <% } %>
                                <form class="d-flex mr-auto">

                                    <span class="btn btn-dark mx-2 text-white"> <%= user.name %> </span>
                                    <!-- <a class="nav-link active back-btn" aria-current="page" onclick="window.history.go(-1)">
                                        <img src="/images/back.svg" class="back-img" alt="bacl arrow"></a> -->


                                    <div class="d-grid">
                                        <a href="/logout" class="btn btn-danger">تسجيل الخروج</a>
                                    </div>
                                </form>
                                <% } %>
                            </div>
                        </div>
                        <% if (user) { %>
                            <% if (user.isAdmin || user.isEditor) { %>
                        <%- include('../navslid')  %> 
                        <% } %>
                        <% } %>
        </nav>



    </header>
